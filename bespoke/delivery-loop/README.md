# Delivery-loop has moved

The maintained implementation is now in the
[Engineering Workflow plugin](../../plugins/engineering-workflow/README.md).
This directory is a compatibility landing page, not another skill copy.

Use [distribution and updates](../../docs/distribution.md) to install a pinned
release. Historical commits retain the previous standalone package.
